<form method="post" action="{{ path('plainpage_delete', {'id': plainpage.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plainpage.id) }}">
    <button class="btn btn-danger pull-right">Удалить</button>
</form>
<form method="post" action="{{ path('plainpage_delete', {'id': plainpage.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plainpage.id) }}">
    <button class="btn btn-danger pull-right">Удалить</button>
</form>
