{% extends 'admin/index.html.twig' %}
  {% block content %}

      <div class="content-wrapper" >
          <!-- Content Header (Page header) -->
          <section class="content-header" >
              <h1 >
                  Обратная связь

              </h1 >
              <ol class="breadcrumb" >
                  <li ><a href="#" ><i class="fa fa-dashboard" ></i > Home</a ></li >
                  <li class="active" >Обратная связь</li >
              </ol >
          </section >

          <!-- Main content -->
          <section class="content" >
              <!-- /.row -->
              <!-- Main row -->

              <div class="row" >
                  <div class="col-md-6" >
                      <div class="box box-warning" >

                          <div class="box-body" >


                                  <!-- text input -->
                                  <div class="form-group">
                                      <label>Дата:</label>

                                      <div class="input-group">
                                        {{ feedback.date|date('d.m.Y') }}
                                      </div>
                                      <!-- /.input group -->
                                  </div>
                                  <div class="form-group" >
                                      <label >Тип</label >
                                         {{ feedback.type}}
                                  </div >

                                  <div class="form-group">
                                      <label>Файл</label>
                                        {{ feedback.text}}

                                  </div>
                                  <div class="form-group">
                                      <label for="preview">Превью</label>
                                      {{ feedback.file }}
                                  </div>
                                  <div class="form-group">
                                      <label for="preview">Email</label>
                                      {{ feedback.email }}
                                  </div>
                                  <div class="form-group">
                                      <label for="preview">Телефон</label>
                                      {{ feedback.phone }}
                                  </div>

                                  <div class="box-footer" >


                                      {% if feedback is defined %}
                                          <a href="{{ path('feedback_delete',{'id':feedback.id}) }}"><span type="submit" class="btn btn-danger pull-right " >Удалить</span ></a>
                                      {% endif %}
                                  </div >

                          </div >
                      </div >
                  </div >
              </div >


          </section >

          <!-- /.content -->
  {% endblock %}

          {% block jsinline %}
          <script>
              $(function () {
                  CKEDITOR.replace('editor1')
                  //Date picker
                  $('#datepicker').datepicker({
                      autoclose: true,

                  })
                  $('#datepicker2').datepicker({
                      autoclose: true
                  });
                  $('.select2').select2()
              })
            function update(type) {
                $('#'+type+'_upload').html('');
                $('#news_redact_'+type+'_upload').removeClass('hide');
                $('#news_redact_'+type+'_upload').click();
            }
          </script>
          {% endblock %}