{% extends 'admin/index.html.twig' %}
  {% block content %}
<div class="content-wrapper" >
    <!-- Content Header (Page header) -->
    <section class="content-header" >
        <h1 >
            Страницы

        </h1 >
        <ol class="breadcrumb" >
            <li ><a href="#" ><i class="fa fa-dashboard" ></i > Home</a ></li >
            <li class="active" >Страницы</li >
        </ol >
    </section >

    <!-- Main content -->
    <section class="content" >
        <!-- /.row -->
        <!-- Main row -->

        <div class="row" >
            <div class="col-md-12" >
                <div class="box box-warning" >

                    <div class="box-body">
                        <table id="example2" class="table table-bordered table-hover">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Преимущество</th>
                                <th>Активность</th>
                                <th>Сортировка</th>
                                {#<th>Удалить</th>#}
                            </tr>
                            </thead>
                            <tbody>
                            {% for plus in pluses %}
                                <tr onclick="window.location.href='{{ path('plus_edit',{'id':plus.id}) }}'; return false">
                                    <td>{{ loop.index }}</td>
                                    <td>{{ plus.plus }}</td>
                                    <td>{{ plus.active ? 'Да' : 'Нет' }}</td>
                                    <td>{{ plus.sort }}</td>
                                    {#<td>#}
                                        {#{{ include('plus/_delete_form.html.twig') }}#}
                                    {#</td>#}
                                </tr>
                            {% endfor %}

                            </tbody>
                        </table>
                    </div>
                </div >
            </div >
        </div >
        <div class="row">
            <div class="col-md-2" >
                <a href="{{ path('plus_new') }}">
                    <span class="btn btn-block btn-primary">Создать</span>
                </a>
            </div>
        </div>



    </section >

    <!-- /.content -->
    {% endblock %}

